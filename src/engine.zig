pub const Window = @import("engine/Window.zig");
pub const texture = @import("engine/texture.zig");
pub const state = @import("engine/state.zig");

pub const init = state.init;
pub const destroy = state.destroy;
